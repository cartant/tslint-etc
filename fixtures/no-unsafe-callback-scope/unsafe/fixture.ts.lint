let outer: any;
export const result = [1].map(value => outer = value);
                                       ~~~~~            [no-unsafe-callback-scope]

[no-unsafe-callback-scope]: Unsafe scopes are forbidden
